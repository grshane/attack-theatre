--- cpn.module	2014-06-07 07:34:25.000000000 -0400
+++ cpn.module	2014-12-03 13:16:15.029523227 -0500
@@ -204,7 +204,8 @@
     $form['cpn']['js'] = array(
       '#type' => 'textarea',
       '#title' => t('JavaScript'),
-      '#default_value' => $cpn['js'],
+//      '#default_value' => $cpn['js'],
+      '#default_value' => file_get_contents('public://cpn/videobg.js'),
       '#description' => t('Custom JavaScript for this node. Do not include @script tags.', array('@script' => '<script>')),
     );
     $title[] = 'JavaScript';
@@ -462,7 +463,7 @@
   else {
     foreach (array('css', 'js') as $type) {
       $file = variable_get('cpn_path', 'public://cpn') . '/' . $node->nid . '.' . $type;
-      if (is_file($file)) {
+	if (is_file($file)) {
         $node->content['#attached'][$type]['cpn_node_' . $node->nid] = array(
           'type' => 'file',
           'group' => $type == 'css' ? CSS_THEME : JS_THEME,
@@ -847,3 +848,4 @@
 
   return $output;
 }
+
