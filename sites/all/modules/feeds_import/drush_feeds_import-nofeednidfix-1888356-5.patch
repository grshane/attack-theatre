diff --git a/drush_feeds_import.drush.inc b/drush_feeds_import.drush.inc
index 508ed1d..e43d4ca 100644
--- a/drush_feeds_import.drush.inc
+++ b/drush_feeds_import.drush.inc
@@ -15,9 +15,12 @@ function drush_feeds_import_drush_command() {
       'feed-id' => array(
         'description' => dt('Feed ID to import.'),
       ),
+      'feed-node-id' => array(
+        'description' => dt('Feed Node ID to import.'),
+      ),
     ),
     'examples' => array(
-      'drush feeds-import --feed-id=feed_importer_1' => "Define the feed-id to process in background.\nIf feed is not configured to run in background will be skipped.",
+      'drush feeds-import --feed-id=feed_importer_1 --feed-node-id=1234' => "Define the feed-id to process in background.\nIf feed is not configured to run in background will be skipped.",
     ),
   );
 
@@ -35,9 +38,15 @@ function drush_feeds_import_drush_command() {
 /**
  * Import a individual feed.
  */
-function _feed_import($id) {
-  $feed = db_query("SELECT feed_nid FROM {feeds_source} WHERE id = :id", array(':id' => $id))->fetchObject();
-  if ($feed->feed_nid) {
+function _feed_import($id, $nid = NULL) {
+  $query = db_select('feeds_source', 'fs')
+    ->fields('fs', array('id', 'feed_nid'))
+    ->condition('fs.id', $id);
+  if($nid) {
+    $query->condition('fs.feed_nid', $nid);
+  }
+  $feeds = $query->execute();
+  foreach($feeds as $feed) {
     $feed_obj = feeds_source($id, $feed->feed_nid);
     $feed_config = $feed_obj->importer->getConfig();
     if ($feed_config['process_in_background']) {
@@ -47,8 +56,8 @@ function _feed_import($id) {
       drush_log(dt('Feed @id is not enabled to Run in Background', array('@id' => $id)), 'error');
     }
   }
-  else {
-    drush_log(dt('Invalid Feed ID: @id', array('@id' => $id)), 'error');
+  if(empty($feeds)) {
+    drush_log(dt('Invalid Feed ID: @id, Feed NID: @nid', array('@id' => $id, '@nid' => $nid)), 'error');
   }
 }
 
@@ -68,8 +77,9 @@ function drush_drush_feeds_import_feeds_import_all() {
  */
 function drush_drush_feeds_import_feeds_import() {
   $id = drush_get_option('feed-id');
+  $nid = drush_get_option('feed-node-id');
   if ($id) {
-    _feed_import($id);
+    _feed_import($id, $nid);
   }
   else {
     drush_log(dt('Feed ID is required. Usage: drush feeds-import #ID.'), 'error');
diff --git a/drush_feeds_import.module b/drush_feeds_import.module
index e69de29..b3d9bbc 100644
--- a/drush_feeds_import.module
+++ b/drush_feeds_import.module
@@ -0,0 +1 @@
+<?php
